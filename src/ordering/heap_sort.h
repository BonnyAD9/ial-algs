#ifndef ORDERING_HEAP_SORT_H_INCLUDED
#define ORDERING_HEAP_SORT_H_INCLUDED

#include <stddef.h>

// time:    O(n log n)
// memory:  O(1)
// stable:  no
// natural: no
void heap_sort(int *arr, size_t len);

#endif // ORDERING_HEAP_SORT_H_INCLUDED
